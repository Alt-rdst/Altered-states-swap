# 1. (Re)set the GitHub repo as your remote origin
git remote add origin https://github.com/alt-rdst/altered-states-swap.git

# 2. Set your branch to main (in case it’s not already)
git branch -M main

# 3. Force push to overwrite the GitHub repo
git push -u origin main --force
